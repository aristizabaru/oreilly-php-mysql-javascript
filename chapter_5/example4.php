<?php // Include and require

/*
include -> includes an external script. It doesn't stop
execution if the script is not found.

include_once -> includes only one instance of the script,
it doesn't matter if it is reloaded in another sentence

require -> same as include but it throws an error if
the file doesn't exist

require_once -> same as include
*/

require 'example1.php';
